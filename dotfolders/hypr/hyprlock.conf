# Backgrounds
background {
    monitor = DP-2
    path = /home/levi/Pictures/wallpapers/micheals/space/astronaut.png
}

background {
    monitor = DP-1
    color = rgba(0, 0, 0, 1.0)
}

background {
    monitor = HDMI-A-1
    color = rgba(0, 0, 0, 1.0)
}

Input field (only on DP-2)
input-field {
    monitor = DP-2
    size = 200
    outline-thickness = 2
    dots-size = 0.3
    dots-spacing = 0.2
    dots-rounding = 1.0
    inner-color = rgba(0, 0, 0, 0.5)
    outer-color = rgba(255, 255, 255, 0.5)
    outline-color = rgba(255, 255, 255, 0.7)
    fade-on-empty = true
    placeholder-text = Enter Password
    placeholder-color = rgba(200, 200, 200, 0)
    check-color = rgba(0, 255, 0, 0.7)
    fail-color = rgba(255, 0, 0, 0.7)
}

# (Optional) Avatar only on DP-2
# User greeting
label {
    monitor = DP-2
    text = Hello, $USER 👋
    font_family = JetBrainsMono Nerd Font
    font_size = 28
    color = rgba(ffffffcc)
    valign = top
    halign = center
    position = 0, -160
}


# Backgrounds
background {
    monitor = DP-2
    path = /home/levi/Pictures/wallpapers/micheals/space/astronaut.png
}

background {
    monitor = DP-1
    color = rgba(0, 0, 0, 1.0)
}

background {
    monitor = HDMI-A-1
    color = rgba(0, 0, 0, 1.0)
}

# Clock only on DP-2
clock {
    monitor = DP-2
    format = %H:%M
    valign = center
    halign = center
    text-color = rgba(255,255,255,0.8)
}

# Input field only on DP-2
input-field {
    monitor = DP-2
    size = 200
    outline-thickness = 2
    dots-size = 0.3
    dots-spacing = 0.2
    dots-rounding = 1.0
    inner-color = rgba(0, 0, 0, 0.5)
    outer-color = rgba(255, 255, 255, 0.5)
    outline-color = rgba(255, 255, 255, 0.7)
    fade-on-empty = false
    placeholder-text = Enter Password
    placeholder-color = rgba(200, 200, 200, 0.6)
    check-color = rgba(0, 255, 0, 0.7)
    fail-color = rgba(255, 0, 0, 0.7)
}

# Optional: Avatar only on DP-2
avatar {
    monitor = DP-2
    size = 100
}
